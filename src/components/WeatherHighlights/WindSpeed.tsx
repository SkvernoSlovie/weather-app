import React from 'react';
import styled from 'styled-components';
import Card from './Card';

const WindSpeed: React.FC = () => {
  return <Card title="Wind Speed"></Card>;
};

export default WindSpeed;
